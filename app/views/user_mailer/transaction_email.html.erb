<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>Hi, <%= @user.first_name %></p>
      <p>
        Your instance <%= @product.name + ' (' + @product.get_value + ')' %> with Instance Id (<%= @product.product_id %>) has been launched successfully.
      </p>
      <p>
        You instance will be ready with in half an hour and you can access it at <%= @product.dns_name %>
      </p>
  </body>
</html>